- args:
    sql: CREATE TABLE "category"("id" serial NOT NULL, "title" text NOT NULL, "article_id"
      integer NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: category
  type: add_existing_table_or_view
