import React from 'react';

const PortfolioFilterItem = props => {

  // handleChange: function(val) {
  //   this.setState({sex: val});
  //   var filteredData;
  //   if(val == ""){
  //     filteredData = this.props.data;
  //   }else{
  //     filteredData = this.props.data.filter(function(item) {
  //     return item.sex === val;
  //   });
  // }
  //
  // let categories;
  //
  // if (props.categories.length > 0) {
  //   categories = props.categories.map(cat => <li key={cat.id}><a href={cat.id}>{cat.title}</a></li>)
  // } else {
  //   categories = 'No categories';
  // }
  //
  // return(
  //   <ul className="dropdown-menu" role="menu" onChange={this.handleChange}>
  //       {categories}
  //   </ul>
  // );
}

export default PortfolioFilterItem;
